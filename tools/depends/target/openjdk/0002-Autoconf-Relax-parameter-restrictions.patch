---
 make/autoconf/basics.m4    | 1 +
 make/autoconf/toolchain.m4 | 5 -----
 2 files changed, 1 insertion(+), 5 deletions(-)

diff --git a/make/autoconf/basics.m4 b/make/autoconf/basics.m4
index 3f9994b832..8e2507a53b 100644
--- a/make/autoconf/basics.m4
+++ b/make/autoconf/basics.m4
@@ -275,6 +275,7 @@ AC_DEFUN([BASIC_FIXUP_EXECUTABLE],
       # First separate the path from the arguments. This will split at the first
       # space.
       complete="[$]$1"
+      complete="${complete## }" # Strip leading space
       path="${complete%% *}"
       tmp="$complete EOL"
       arguments="${tmp#* }"
diff --git a/make/autoconf/toolchain.m4 b/make/autoconf/toolchain.m4
index 91fabcd4f4..ef33947611 100644
--- a/make/autoconf/toolchain.m4
+++ b/make/autoconf/toolchain.m4
@@ -514,11 +514,6 @@ AC_DEFUN([TOOLCHAIN_FIND_COMPILER],
       else
         AC_MSG_ERROR([User supplied compiler $1=[$]$1 could not be found])
       fi
-    else
-      # Otherwise it might already be a complete path
-      if test ! -x "[$]$1"; then
-        AC_MSG_ERROR([User supplied compiler $1=[$]$1 does not exist])
-      fi
     fi
   else
     # No user supplied value. Locate compiler ourselves.
-- 
